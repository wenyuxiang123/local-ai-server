<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- 扣子风格主题 -->
    <style name="Theme.LocalAI" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorPrimaryVariant">@color/primary_variant</item>
        <item name="colorAccent">@color/accent</item>
        <item name="colorSecondary">@color/accent</item>
        <item name="android:windowBackground">@color/background_chat</item>
        <item name="android:statusBarColor">@color/toolbar_bg</item>
        <item name="android:navigationBarColor">@color/bottom_nav_bg</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- 扣子风格 Toolbar -->
    <style name="CozeToolbar" parent="Widget.MaterialComponents.Toolbar">
        <item name="android:background">@color/toolbar_bg</item>
        <item name="titleTextColor">@color/toolbar_text</item>
        <item name="android:elevation">4dp</item>
    </style>

    <!-- 圆角卡片样式 -->
    <style name="CozeCard" parent="Widget.MaterialComponents.CardView">
        <item name="cardCornerRadius">12dp</item>
        <item name="cardElevation">2dp</item>
        <item name="cardBackgroundColor">@color/card_background</item>
    </style>

    <!-- 圆角按钮样式 -->
    <style name="CozeButton" parent="Widget.MaterialComponents.Button">
        <item name="cornerRadius">24dp</item>
        <item name="backgroundTint">@color/primary</item>
    </style>

    <!-- 圆角输入框样式 -->
    <style name="CozeTextInput" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxCornerRadiusTopStart">24dp</item>
        <item name="boxCornerRadiusTopEnd">24dp</item>
        <item name="boxCornerRadiusBottomStart">24dp</item>
        <item name="boxCornerRadiusBottomEnd">24dp</item>
    </style>

    <!-- 侧边栏菜单项样式 -->
    <style name="DrawerMenuItem">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:background">?attr/selectableItemBackground</item>
        <item name="android:textColor">@color/drawer_item_text</item>
        <item name="android:textSize">14sp</item>
    </style>

    <!-- 侧边栏分组标题 -->
    <style name="DrawerSectionTitle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:paddingTop">16dp</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:textColor">@color/text_secondary</item>
        <item name="android:textSize">12sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- 聊天气泡 - AI消息 -->
    <style name="BubbleAI">
        <item name="android:background">@drawable/bg_message_assistant</item>
        <item name="android:textColor">@color/message_assistant_text</item>
        <item name="android:textSize">15sp</item>
        <item name="android:padding">12dp</item>
    </style>

    <!-- 聊天气泡 - 用户消息 -->
    <style name="BubbleUser">
        <item name="android:background">@drawable/bg_message_user</item>
        <item name="android:textColor">@color/message_user_text</item>
        <item name="android:textSize">15sp</item>
        <item name="android:padding">12dp</item>
    </style>

    <!-- 底部输入框样式 -->
    <style name="ChatInputStyle">
        <item name="android:background">@drawable/bg_input</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textColorHint">@color/input_hint</item>
        <item name="android:textSize">15sp</item>
        <item name="android:maxLines">4</item>
    </style>
</resources>
